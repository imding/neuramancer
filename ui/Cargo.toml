[package]
name = "ui"
version = "0.1.0"
edition = "2024"

[dependencies]
backend = { workspace = true, features = ["web"] }

schema = { workspace = true, features = ["web"] }

dioxus.workspace = true
