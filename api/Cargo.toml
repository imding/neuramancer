[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
schema.workspace = true

dioxus = { workspace = true, features = ["fullstack"] }
qdrant-client = { version = "1.15.0", optional = true}

[features]
web = ["schema/web"]
server = ["schema/server", "dep:qdrant-client"]
